/*
Name: index.ts

Description: Imports all models and exports them neatly.
Example use: import models from "models"; const user = models.User

Author: Johannes Natunen, https://github.com/jopu-n
*/

import User from "./user";

const models = {
  User,
};

export default models;
